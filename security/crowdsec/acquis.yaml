# ============================================================
# CrowdSec — Log Acquisition Configuration
# ============================================================

filenames:
  - /var/log/traefik/access.log
labels:
  type: traefik

---
filenames:
  - /var/log/traefik/traefik.log
labels:
  type: syslog
